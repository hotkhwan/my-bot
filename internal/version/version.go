// Package version exposes the running app version so the dashboard can show
// which build is live. Bump Version on every release.
package version

// Version is the human-readable app version, surfaced in /api/me and shown on
// the dashboard Home screen. Bump it (semver) whenever you ship.
const Version = "0.9.55"
