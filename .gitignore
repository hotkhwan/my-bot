# Secrets and local config
.env
.env.*
!.env.example

# Go build/test artifacts
bin/
# Only the repo-root frontend scratch build is ignored. The embedded dashboard
# build under internal/dashboard/dist IS committed: go:embed needs it present at
# `go build` time (Docker/CI build from a clean checkout).
/dist/
coverage.out
*.test
*.exe
*.out

# Local runtime artifacts
logs/
tmp/
*.log

# IDE/system
.DS_Store
Thumbs.db
.vscode/
.idea/
